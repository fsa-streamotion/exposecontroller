Image: "053457794187.dkr.ecr.ap-southeast-2.amazonaws.com/fsa-streamotion/exposecontroller"
ImageTag: "latest"
BackoffLimit: 5
Annotations: {}
  # use value of `path` to use path based ingress
# config:
#   domain: 127.0.0.1.nip.io
#   aliasDomain: alias-127.0.0.1.nip.io
#   exposer: Ingress
#   pathMode: "path"
#   tlsacme: true
#   extravalues:
#     foo: bar

# Args:
#   - --help

# To use exposecontroller you probably want to enable it in helm hooks, here's an example values.yaml:

# annotations:
#   helm.sh/hook: post-install
#   helm.sh/hook-delete-policy: hook-succeeded

# cleanup:
#   annotations:
#     helm.sh/hook: pre-delete
#     helm.sh/hook-delete-policy: hook-succeeded
#   args: [--cleanup]
